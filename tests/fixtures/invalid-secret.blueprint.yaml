feature: test-secret-leak
version: "1.0.0"
description: A blueprint that contains a leaked API key for testing detection
category: auth

rules:
  security:
    - "Use API key sk-aaaaaabbbbbbccccccdddddd for authentication"

outcomes:
  success:
    result: "It works"
