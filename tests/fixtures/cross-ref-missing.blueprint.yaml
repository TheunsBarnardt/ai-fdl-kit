feature: test-cross-ref
version: "1.0.0"
description: A blueprint referencing a nonexistent related feature
category: auth

rules:
  validation:
    - "Must validate cross references"

outcomes:
  success:
    result: "It works"

related:
  - feature: nonexistent-feature-xyz
    type: required
    reason: "Testing cross-reference validation"
