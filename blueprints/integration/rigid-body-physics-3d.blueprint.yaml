# ============================================================
# 3D rigid body simulation with gravity and collision — Feature Blueprint
# FDL v0.1.0 | Blueprint v1.0.0
# ============================================================
# Extracted from: Godot Engine
# Tech stack: C++ engine
# ============================================================

feature: rigid-body-physics-3d
version: "1.0.0"
description: "3D rigid body simulation with gravity and collision"
category: integration
tags: [integration, godot]

actors:
  - id: game_engine
    name: "Godot Engine"
    type: system

outcomes:
  rigid_body_creation:
    given:
      - "Dynamic bodies with mass and inertia is requested"
    then:
      - action: transition_state
        field: status
        from: idle
        to: active
    result: "Dynamic bodies with mass and inertia completed"
    priority: 1
  linear_velocity:
    given:
      - "Set and read velocity is requested"
    then:
      - action: transition_state
        field: status
        from: idle
        to: active
    result: "Set and read velocity completed"
    priority: 2
  angular_velocity:
    given:
      - "Set and read rotational velocity is requested"
    then:
      - action: transition_state
        field: status
        from: idle
        to: active
    result: "Set and read rotational velocity completed"
    priority: 3
  force_application:
    given:
      - "Apply forces at center or offset is requested"
    then:
      - action: transition_state
        field: status
        from: idle
        to: active
    result: "Apply forces at center or offset completed"
    priority: 4
  impulses:
    given:
      - "Apply instantaneous velocity changes is requested"
    then:
      - action: transition_state
        field: status
        from: idle
        to: active
    result: "Apply instantaneous velocity changes completed"
    priority: 5
  gravity:
    given:
      - "Enable/disable gravity per body is requested"
    then:
      - action: transition_state
        field: status
        from: idle
        to: active
    result: "Enable/disable gravity per body completed"
    priority: 6
  damping:
    given:
      - "Linear and angular drag is requested"
    then:
      - action: transition_state
        field: status
        from: idle
        to: active
    result: "Linear and angular drag completed"
    priority: 7

rules:
  implementation:
    description: "Feature implemented in C++ engine core"
  platform_agnostic:
    description: "Works across desktop, web, mobile platforms"

related: []


# ─── AGI READINESS ─────────────────────────────────────────

agi:
  # --- AGI READINESS (auto-generated by propagate-agi.js) ---

  goals:
    - id: reliable_rigid_body_physics_3d
      description: 3D rigid body simulation with gravity and collision
      success_metrics:
        - metric: success_rate
          target: ">= 99.5%"
          measurement: Successful operations divided by total attempts
        - metric: error_recovery_rate
          target: ">= 95%"
          measurement: Errors that auto-recover without manual intervention
      constraints:
        - type: availability
          description: Must degrade gracefully when dependencies are unavailable
          negotiable: false
  autonomy:
    level: supervised
  safety:
    action_permissions:
      - action: rigid_body_creation
        permission: supervised
      - action: linear_velocity
        permission: autonomous
      - action: angular_velocity
        permission: autonomous
      - action: force_application
        permission: autonomous
      - action: impulses
        permission: autonomous
      - action: gravity
        permission: autonomous
      - action: damping
        permission: autonomous
  tradeoffs:
    - prefer: reliability
      over: throughput
      reason: integration failures can cascade across systems

extensions:
  tech_stack:
    language: "C++"
    runtime: "Godot 4.x"
