feature: test-placeholder
version: "1.0.0"
description: TODO fill this in later
category: auth

rules:
  validation:
    - "Some validation rule for testing"

outcomes:
  success:
    result: "It works"
