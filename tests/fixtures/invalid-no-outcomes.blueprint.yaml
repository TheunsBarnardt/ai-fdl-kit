feature: test-no-outcomes
version: "1.0.0"
description: A blueprint with rules but no outcomes or flows
category: data

rules:
  validation:
    - "Data must be validated"
    - "All inputs must be sanitized"
