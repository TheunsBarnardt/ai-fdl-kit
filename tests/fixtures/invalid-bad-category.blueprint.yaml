feature: test-bad-category
version: "1.0.0"
description: A blueprint with an invalid category value
category: nonexistent-category

rules:
  validation:
    - "Some validation rule"

outcomes:
  success:
    result: "It works"
