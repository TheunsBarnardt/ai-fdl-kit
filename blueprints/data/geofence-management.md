<!-- AUTO-GENERATED FROM geofence-management.blueprint.yaml — DO NOT EDIT. Run `npm run generate:readmes` to refresh. -->

# Geofence Management

> Define named geographic boundary zones as circles (centre point + radius) or polygons (closed coordinate ring), optionally with altitude constraints and calendar-based activation schedules, and eva...

**Category:** Data · **Version:** 1.0.0 · **Tags:** gps · tracking · geofence · zone · polygon · circle · fleet

## What this does

Define named geographic boundary zones as circles (centre point + radius) or polygons (closed coordinate ring), optionally with altitude constraints and calendar-based activation schedules, and eva...

Specifies 5 acceptance outcomes that any implementation must satisfy, regardless of language or framework.

## Fields

- **name** *(text, required)* — Human-readable name for the zone
- **description** *(text, optional)* — Optional notes about the purpose or location of the zone
- **area** *(text, required)* — Geometry definition in a portable text format. Circle: CIRCLE(lat lon, radiusMetres) Polygon: POL...
- **geometry_type** *(select, optional)* — Derived geometry type: circle or polygon (determined from area field)
- **altitude_floor** *(number, optional)* — Minimum altitude (metres above sea level) for the zone to apply; positions below this altitude ar...
- **altitude_ceiling** *(number, optional)* — Maximum altitude (metres above sea level) for the zone to apply
- **speed_limit** *(number, optional)* — Optional speed limit (in the platform's configured speed unit) that applies within this zone, ove...
- **calendar_id** *(hidden, optional)* — Reference to a calendar record; when set, the zone is only active during the calendar's scheduled...

## What must be true

- **processing → circle_containment:** A circle geofence is defined by a centre coordinate and a radius in metres; containment is determined by the great-circle distance from the position to the centre
- **processing → polygon_containment:** A polygon geofence uses a ray-casting (even-odd) algorithm to determine if the position point lies inside the closed ring
- **processing → altitude_constraints:** If altitude_floor or altitude_ceiling are set, a position is only considered inside the zone if its altitude satisfies the constraint
- **processing → calendar_activation:** If a calendar is associated, the zone is only considered active during calendar periods; positions received outside active periods are not evaluated against that zone
- **processing → speed_limit_override:** A zone may carry a speed_limit attribute; the platform uses the zone's speed limit in preference to the device-level limit when the device is inside the zone
- **data → multiple_zones_concurrent:** Multiple zones can be active simultaneously for a single device; a position can be inside zero or more zones at the same time
- **data → zone_ids_recorded_on_position:** The list of active zone IDs is recorded on each position record so historical queries can reconstruct zone membership at any point in time

## Success & failure scenarios

**✅ Success paths**

- **Position Inside Zone** — when an incoming position is evaluated and the point lies within the zone boundary; zone calendar is active (or no calendar is set); position altitude is within floor and ceiling constraints (if set), then Zone ID recorded on position; geofence alert handler can detect entry transitions.
- **Geofence Deleted** — when fleet_admin deletes a geofence zone, then Zone is removed; no further entry or exit events will be generated for it.
- **Geofence Updated** — when fleet_admin submits updated name, area, or attributes; geofence_id exists, then Zone definition updated; subsequent position evaluations use the new geometry.
- **Geofence Created** — when fleet_admin provides name and valid area geometry, then Zone is saved and immediately evaluated against future incoming positions.

**❌ Failure paths**

- **Invalid Geometry** — when area string does not parse as a valid circle or polygon, then Geofence is not saved; operator receives a geometry validation error. *(error: `GEOFENCE_INVALID_GEOMETRY`)*

## Errors it can return

- `GEOFENCE_INVALID_GEOMETRY` — The zone boundary definition is invalid; check coordinate format and closure
- `GEOFENCE_NOT_FOUND` — The specified geofence does not exist

## Connects to

- **geofence-alerts** *(required)* — Entry and exit events are generated by comparing zone memberships between consecutive positions
- **gps-position-ingestion** *(required)* — Positions must be ingested and enriched with zone membership before alert evaluation
- **overspeed-alerts** *(recommended)* — Zone-level speed limits override device-level limits for overspeed detection

## Quality fitness 🟢 76/100

Automated quality score measuring outcome coverage, rule structure, error binding, and field validation depth. Regenerated by `npm run fitness` — see [`scripts/fitness.js`](../../scripts/fitness.js) for the scoring model.

| Dimension | Score | Points |
|-----------|-------|--------|
| Description | `██████████` | 10/10 |
| Rules | `████████░░` | 8/10 |
| Outcomes | `███████████████████████░░` | 23/25 |
| Structured conditions | `█████░░░░░` | 5/10 |
| Error binding | `██████░░░░` | 6/10 |
| Field validation | `█████░░░░░` | 5/10 |
| Relationships | `█████████░` | 9/10 |
| Events | `█████` | 5/5 |
| AGI readiness | `░░░░░` | 0/5 |
| Simplicity | `█████` | 5/5 |

---

**Full reference:** [docs site](https://theunsbarnardt.github.io/ai-fdl-kit/blueprints/data/geofence-management/) · **Spec source:** [`geofence-management.blueprint.yaml`](./geofence-management.blueprint.yaml)

*Generated from YAML — any edits to this file will be overwritten. Update the blueprint YAML and re-run `npm run generate:readmes`.*
