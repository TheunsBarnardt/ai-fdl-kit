<!-- AUTO-GENERATED FROM payments-gateway-api.blueprint.yaml — DO NOT EDIT. Run `npm run generate:readmes` to refresh. -->

# Payments Gateway Api

> Central HTTP surface for all payment operations — thin-client terminals and admin consoles call this; PGW owns rail selection, EMV, fraud, refunds, disputes

**Category:** Integration · **Version:** 1.0.0 · **Tags:** gateway · api · payments · rails · emv · fraud · refund · dispute

## What this does

Central HTTP surface for all payment operations — thin-client terminals and admin consoles call this; PGW owns rail selection, EMV, fraud, refunds, disputes

Specifies 7 acceptance outcomes that any implementation must satisfy, regardless of language or framework.

## Fields

- **payment_id** *(token, required)* — Payment ID
- **merchant_id** *(text, required)* — Merchant ID
- **amount** *(number, required)* — Amount (cents)
- **currency** *(text, required)* — ISO-4217 currency code
- **method** *(select, required)* — Payment method
- **palm_session_token** *(token, optional)* — Palm session token (when method=palm)
- **card_data_encrypted** *(text, optional)* — SPoC-encrypted card data (when method=card_*)
- **device_attestation** *(text, required)* — Signed device attestation — validated per call
- **idempotency_key** *(token, required)* — Client-generated idempotency key

## What must be true

- **security:** MUST: mTLS + device attestation verified on every request, MUST: idempotency_key deduplicates retries within 24h, MUST: card data is SPoC-encrypted end-to-end; PGW decrypts inside HSM boundary only, MUST: response never includes full PAN or CVV
- **routing:** MUST: rail selection runs through the Rail Registry policy engine, not hardcoded, MUST: on rail failure, return 502 with PGW_RAIL_ERROR — do not fall back silently
- **observability:** MUST: emit payment.initiated / payment.settled / payment.failed events for audit and metrics

## Success & failure scenarios

**✅ Success paths**

- **Successful Palm Payment** — when method == palm; palm_session_token valid and unexpired; device attestation verified; rail routing succeeds, then Payment settled via selected rail; receipt queued.
- **Successful Card Payment** — when method in [card_chip, card_tap, card_stripe]; cloud EMV kernel authorises; rail routing succeeds, then Card payment authorised and captured.

**❌ Failure paths**

- **Attestation Failed** — when device attestation signature invalid or expired, then 401 — device attestation rejected; no rail call made. *(error: `PGW_ATTESTATION_FAILED`)*
- **Rate Limited** — when requests_in_window gt 100, then 429 — request throttled. *(error: `PGW_RATE_LIMITED`)*
- **Idempotency Conflict** — when idempotency_key seen within 24h with different payload, then 409 — conflicting retry. *(error: `PGW_IDEMPOTENCY_CONFLICT`)*
- **Palm Session Expired** — when method == palm; palm_session_token expired or not found, then 422 — resolve palm session again. *(error: `PGW_PALM_SESSION_EXPIRED`)*
- **Rail Error** — when rail adapter returns non-success after retry, then 502 — rail error surfaced to caller; transaction not settled. *(error: `PGW_RAIL_ERROR`)*

## Errors it can return

- `PGW_INVALID_REQUEST` — Invalid payment request
- `PGW_ATTESTATION_FAILED` — Device authentication failed
- `PGW_IDEMPOTENCY_CONFLICT` — Duplicate request with different payload
- `PGW_PALM_SESSION_EXPIRED` — Palm session expired — please rescan
- `PGW_RATE_LIMITED` — Too many requests — retry later
- `PGW_RAIL_ERROR` — Payment network error

## Events

**`payment.initiated`** — Payment request accepted and queued for rail routing

**`payment.settled`** — Payment confirmed by rail

**`payment.failed`** — Payment rejected or errored at any stage

**`payment.rate_limited`**

**`payment.attestation_failed`**

**`payment.rail_error`**

## Connects to

- **popia-compliance** *(required)* — Handles personal information of SA data subjects — must satisfy POPIA Act 4 of 2013
- **rail-registry** *(required)* — Rail selection is delegated to the registry — PGW never hard-codes rails
- **cloud-emv-kernel** *(required)* — Card payments require cloud EMV — terminal is SPoC passthrough only
- **device-attestation** *(required)* — Every call requires a verified device attestation
- **fraud-detection** *(required)* — Every payment is scored before rail execution
- **palm-pay** *(required)* — Palm sessions resolve to payment proxies via PGW
- **payment-observability** *(recommended)* — Transaction metrics, latency p50/p95/p99, and alerting

## Quality fitness 🟢 80/100

Automated quality score measuring outcome coverage, rule structure, error binding, and field validation depth. Regenerated by `npm run fitness` — see [`scripts/fitness.js`](../../scripts/fitness.js) for the scoring model.

| Dimension | Score | Points |
|-----------|-------|--------|
| Description | `██████████` | 10/10 |
| Rules | `████████░░` | 8/10 |
| Outcomes | `███████████████████████░░` | 23/25 |
| Structured conditions | `█████░░░░░` | 5/10 |
| Error binding | `█████████░` | 9/10 |
| Field validation | `███████░░░` | 7/10 |
| Relationships | `██████████` | 10/10 |
| Events | `███░░` | 3/5 |
| AGI readiness | `░░░░░` | 0/5 |
| Simplicity | `█████` | 5/5 |

---

**Full reference:** [docs site](https://theunsbarnardt.github.io/ai-fdl-kit/blueprints/integration/payments-gateway-api/) · **Spec source:** [`payments-gateway-api.blueprint.yaml`](./payments-gateway-api.blueprint.yaml)

*Generated from YAML — any edits to this file will be overwritten. Update the blueprint YAML and re-run `npm run generate:readmes`.*
