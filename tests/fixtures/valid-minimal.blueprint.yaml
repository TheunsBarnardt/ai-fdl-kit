feature: test-minimal
version: "1.0.0"
description: A minimal valid blueprint for testing purposes
category: auth

rules:
  validation:
    - "Input must be validated before processing"
    - "All fields are required unless marked optional"

outcomes:
  successful:
    given:
      - "valid input is provided"
    then:
      - "process the request"
    result: "Request processed successfully"
