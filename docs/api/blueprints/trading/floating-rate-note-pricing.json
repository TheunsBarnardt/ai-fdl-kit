{
  "feature": "floating-rate-note-pricing",
  "version": "1.0.0",
  "description": "Floating rate note pricing using discount margin methodology and swap zero curve.",
  "category": "trading",
  "tags": [
    "bonds",
    "floating-rate",
    "frn",
    "pricing",
    "money-market",
    "jibar"
  ],
  "fields": [
    {
      "name": "notional",
      "type": "number",
      "required": true,
      "label": "Notional Amount"
    },
    {
      "name": "settlement_date",
      "type": "date",
      "required": true,
      "label": "Settlement Date"
    },
    {
      "name": "maturity_date",
      "type": "date",
      "required": true,
      "label": "Maturity Date"
    },
    {
      "name": "market_spread",
      "type": "number",
      "required": true,
      "label": "Market Spread"
    }
  ],
  "rules": {
    "pricing": {
      "forward_rate": "Fwd_rate = (DF(t)/DF(t+1) - 1) × 365/days",
      "coupon_payment": "Coupon = Nominal × (Forward_Rate + Issue_Spread) × days/365",
      "frn_price": "Price = Sum[DF_zero × (Coupon_i + Principal)]"
    }
  },
  "outcomes": {
    "frn_priced": {
      "description": "Calculate FRN all-in price using discount margin method",
      "given": [
        {
          "field": "market_spread",
          "source": "input",
          "operator": "exists"
        },
        {
          "field": "maturity_date",
          "source": "input",
          "operator": "exists"
        }
      ],
      "then": [
        {
          "action": "set_field",
          "target": "all_in_price",
          "value": "discounted cash flows"
        },
        {
          "action": "emit_event",
          "event": "frn.price.calculated",
          "payload": [
            "settlement_date",
            "all_in_price"
          ]
        }
      ],
      "result": "FRN all-in price calculated"
    }
  },
  "errors": [
    {
      "code": "INVALID_SPREAD",
      "status": 400,
      "message": "Spread values must be non-negative"
    }
  ],
  "related": [
    {
      "feature": "bond-pricing-models",
      "type": "required"
    }
  ]
}